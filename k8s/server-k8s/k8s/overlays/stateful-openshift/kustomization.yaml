apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

components:
  - ../../components/statefulset

patches:
  - path: storageclass-patch.yaml
  - path: openshift-patch.yaml

images:
  - name: dbmarlin/dbmarlin-server
    newTag: 6.3.0 # Change this tag to upgrade DBmarlin
